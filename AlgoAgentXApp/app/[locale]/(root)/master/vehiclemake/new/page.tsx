"use client";

import VehiclemakeForm from "../components/vehiclemake-form";

type Props = {};  
const page = (props: Props) => {
  return (
    <>
      <VehiclemakeForm />
    </>
  );
};

export default page;
